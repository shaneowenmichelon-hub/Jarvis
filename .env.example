# ZMM // SPONSOR COMMAND — environment config
# Copy this to .env and fill in your values. Do NOT commit .env.

# ---- Server ----
PORT=3001
PUBLIC_URL=http://localhost:3001

# ---- Google OAuth (Gmail) ----
# Create OAuth credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must match PUBLIC_URL + /auth/google/callback
# Required scope: https://www.googleapis.com/auth/gmail.readonly
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# ---- Anthropic (JARVIS chat) ----
# Optional. Without this, the chat falls back to simulated mode.
ANTHROPIC_API_KEY=

# ---- ElevenLabs (JARVIS voice) ----
# Optional. Without this, voice falls back to your browser's built-in
# (robotic) TTS. Get a key at https://elevenlabs.io — free tier covers
# ~10K characters/month, plenty for chat use.
ELEVENLABS_API_KEY=
# Optional. Defaults to "George" — a mature British male voice that
# sounds close to JARVIS. Browse other voice IDs at
# https://elevenlabs.io/app/voice-library
ELEVENLABS_VOICE_ID=

# ---- Sync ----
# Minimum seconds between Gmail syncs (rate limit). Default 30.
SYNC_MIN_INTERVAL=30
